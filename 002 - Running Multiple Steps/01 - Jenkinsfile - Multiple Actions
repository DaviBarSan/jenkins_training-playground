pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stage ('build'){
    	steps{
		sh 'date > currDate.txt | cat curDate.txt'
		sh 'sh sleep 10s'
		sh 'cat currDate.txt'
		
	}
    }
}

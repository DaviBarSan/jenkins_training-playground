pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages('build'){
    	steps{
		sh 'date > currDate.txt | cat curDate.txt'
		sh 'sh sleep 10s'
		sh 'cat currDate.txt'
		
	}
    }
}

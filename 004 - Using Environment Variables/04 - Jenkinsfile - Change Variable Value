pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    environment{
        NUMBER_OF_ISSUES=0
    }
    stages {
        stage('test'){
            $NUMBER_OF_ISSUES=10
        }
    }
    always{
        echo {$NUMBER_OF_ISSUES}
    }
}
